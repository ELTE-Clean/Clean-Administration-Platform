{
  "name": "backend",
  "version": "1.0.0",
  "description": "Clean adminstration platform backend",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./npm-debug.log --copy-files",
    "start": "npm run build && npm install && node dist/server.js"
  },
  "dependencies": {
    "body-parser": "^1.13.1",
    "cookie-parser": "^1.3.5",
    "events": "^3.2.0",
    "express": "^4.13.0",
    "express-session": "^1.17.1",
    "express-promise-router": "^4.1.0",
    "qs": "^6.9.4",
    "querystring": "^0.2.0"
   },
   "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^3.0.2"
   }
}
